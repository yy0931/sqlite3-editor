{
  "name": "ui",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "//": "Vite's HMR breaks zustand and import.meta.hmr.decline() didn't work so I switched to a static server",
    "start": "concurrently \"vite build --watch --minify false --sourcemap true\" \"echo 'http://localhost:5173' && python3 -m http.server --bind 127.0.0.1 --directory dist 5173\"",
    "build": "vite build",
    "build-dev": "vite build --minify false --sourcemap true"
  },
  "keywords": [],
  "author": "",
  "license": "",
  "dependencies": {
    "fast-deep-equal": "^3.1.3",
    "msgpackr": "github:yy0931/msgpackr",
    "type-fest": "^3.0.0",
    "use-immer": "^0.7.0",
    "zustand": "^4.1.1"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.4.0",
    "autoprefixer": "^10.4.12",
    "concurrently": "^7.4.0",
    "postcss": "^8.4.16",
    "sass": "^1.54.9",
    "tailwindcss": "^3.1.8",
    "vite": "^3.1.1"
  }
}
